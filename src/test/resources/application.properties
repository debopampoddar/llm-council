# Keep Spring integration tests hermetic. A developer's real overlay under
# ~/.llm-council must never change the catalog or assertions exercised by the
# repository test suite. Tests that intentionally exercise an overlay set
# council.userConfigPath explicitly and take precedence over this default.
council.userConfigPath=target/test-runtime/default-council-user.yml
