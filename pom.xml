<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.debopam</groupId>
  <artifactId>llm-council</artifactId>
  <version>2.0.0</version>
  <name>llm-council</name>
  <description>LLM Council in Java/Spring Boot</description>
  <properties>
    <java.version>25</java.version>
    <maven.compiler.release>25</maven.compiler.release>
    <spring.boot.version>3.5.0</spring.boot.version>
    <spring.ai.version>1.0.0</spring.ai.version>
    <!-- Where `mvn deploy` publishes. Overridable so a fork or a private mirror
         does not need the POM edited: -Dgithub.repository=owner/name -->
    <github.repository>debopampoddar/llm-council</github.repository>
  </properties>

  <!-- GitHub Packages. The id is matched against a <server> in settings.xml, so
       it must stay in step with the credentials block documented in the README.

       Note what publishing here does and does not imply. GitHub Packages
       requires authentication to *download* as well as to publish, even for a
       public package — unlike Maven Central, there is no anonymous read. That
       is a cost for consumers and the reason to choose it deliberately rather
       than by default.

       A released version cannot be overwritten. Re-running deploy for a version
       that already exists fails rather than replacing it, which is the correct
       behaviour and the reason releases bump the version rather than reusing it. -->
  <distributionManagement>
    <repository>
      <id>github</id>
      <name>GitHub Packages</name>
      <url>https://maven.pkg.github.com/${github.repository}</url>
    </repository>
    <snapshotRepository>
      <id>github</id>
      <name>GitHub Packages Snapshots</name>
      <url>https://maven.pkg.github.com/${github.repository}</url>
    </snapshotRepository>
  </distributionManagement>
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring.boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-bom</artifactId>
        <version>${spring.ai.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <dependencies>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-web</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-validation</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-actuator</artifactId></dependency>
    <!-- Durable persistence is JdbcTemplate over a JSON document column, never
         JPA: the domain types are records, and a JPA entity cannot be one. -->
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-jdbc</artifactId></dependency>
    <!-- Flyway versions the schema from the first release that has one. With a
         JSON document column the schema changes almost never, so this is close
         to zero maintenance — and far cheaper than retrofitting versioning onto
         databases that already hold somebody's chat history. -->
    <dependency><groupId>org.flywaydb</groupId><artifactId>flyway-core</artifactId></dependency>
    <!-- SQLite is the recommended engine for personal use — one file, no daemon,
         no port. H2 file mode is the fallback for anyone who would rather not
         take a native dependency. They are not two stores: they are one
         JdbcSessionStore with a different URL and a different driver. Runtime
         scope because no compiled code names either driver. -->
    <dependency><groupId>org.xerial</groupId><artifactId>sqlite-jdbc</artifactId><scope>runtime</scope></dependency>
    <dependency><groupId>com.h2database</groupId><artifactId>h2</artifactId><scope>runtime</scope></dependency>
    <dependency><groupId>org.springframework.ai</groupId><artifactId>spring-ai-starter-model-openai</artifactId></dependency>
    <!-- spring-ai-anthropic declares io.rest-assured:json-path at compile scope,
         which is a test library escaping into a runtime dependency: it brings
         Groovy (~8 MB) with it. Verified unused — no class in any spring-ai
         binary references restassured, jayway, or groovy, and neither does this
         application. Re-check this when upgrading Spring AI. -->
    <dependency>
      <groupId>org.springframework.ai</groupId>
      <artifactId>spring-ai-starter-model-anthropic</artifactId>
      <exclusions>
        <exclusion><groupId>io.rest-assured</groupId><artifactId>json-path</artifactId></exclusion>
      </exclusions>
    </dependency>
    <dependency><groupId>org.springframework.ai</groupId><artifactId>spring-ai-starter-model-ollama</artifactId></dependency>
    <!-- google-cloud-vertexai drags in the whole gRPC service-mesh stack. None of
         it is on the path for direct Vertex API calls: xDS is Envoy-style service
         discovery, grpc-services is server-side reflection/health/channelz, and
         conscrypt is an alternative TLS provider the JDK's own does not need.
         Together they are ~16 MB of a 100 MB jar. -->
    <dependency>
      <groupId>org.springframework.ai</groupId>
      <artifactId>spring-ai-starter-model-vertex-ai-gemini</artifactId>
      <exclusions>
        <exclusion><groupId>io.grpc</groupId><artifactId>grpc-xds</artifactId></exclusion>
        <exclusion><groupId>io.grpc</groupId><artifactId>grpc-services</artifactId></exclusion>
        <exclusion><groupId>io.grpc</groupId><artifactId>grpc-googleapis</artifactId></exclusion>
        <exclusion><groupId>org.conscrypt</groupId><artifactId>conscrypt-openjdk-uber</artifactId></exclusion>
      </exclusions>
    </dependency>
    <!-- Strict YAML binding for the user configuration overlay. snakeyaml alone
         cannot reject unknown fields, which would turn a typo into a silent no-op. -->
    <dependency><groupId>com.fasterxml.jackson.dataformat</groupId><artifactId>jackson-dataformat-yaml</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-test</artifactId><scope>test</scope></dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <version>${spring.boot.version}</version>
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>${maven.compiler.release}</release>
          <parameters>true</parameters>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
